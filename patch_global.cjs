const fs = require('fs');
let code = fs.readFileSync('src/components/GlobalSearchBar.tsx', 'utf8');

// Replace standard Hero and Profile with the precise Screenshot layout
code = code.replace(/<div className="artist-hero">[\s\S]*?<\/div>[\s\S]*?<div className="artist-profile"[\s\S]*?<\/div>\s*<\/div>/, `<div className="artist-hero">
                                    <div className="artist-hero__eyebrow" style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', margin: '0 0 40px 0', padding: isMobile ? '20px' : '40px 50px 0' }}>
                                        <div style={{ display: 'flex', gap: '30px', color: 'var(--ap-accent)', fontSize: '0.65rem', fontWeight: 600, letterSpacing: '0.2em' }}>
                                            <span style={{ border: '1px solid var(--ap-accent)', padding: '6px 12px', borderRadius: 2 }}>ARTIST</span>
                                            <span style={{ padding: '6px 0', opacity: 0.7 }}>ARTIST</spaconst fs = require('fs');
let c  let code = fs.readFileSy  
// Replace standard Hero and Profile with the precise Screenshot layoutylecode = code.replace(/<div className="artist-hero">[\s\S]*?<\/div>[\s\S                                      <div className="artist-hero__eyebrow" style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', margintG                                        <div style={{ display: 'flex', gap: '30px', color: 'var(--ap-accent)', fontSize: '0.65rem', fontWeight: 600, letterSpacing: '0.2em' }}>
                                            <sei                                            <span style={{ border: '1px solid var(--ap-accent)', padding: '6px 12px', borderRadius: 2 }}>ARTIST</span>
                       te                                            <span style={{ padding: '6px 0', opacity: 0.7 }}>ARTIST</spaconst fs = require('fs');
l?'}
                let c  let code = fs.readFileSy  
// Replace standard Hero and Profile with the precise Screenshot layoutylecode = code.replace(  // Replace standard Hero and Pro_t                                            <sei                                            <span style={{ border: '1px solid var(--ap-accent)', padding: '6px 12px', borderRadius: 2 }}>ARTIST</span>
                       te                                            <span style={{ padding: '6px 0', opacity: 0.7 }}>ARTIST</spaconst fs = require('fs');
l?'}
                let c  let code = fs.readFileSy  
// Replace standard Hero and Profile with the precise Screenshot layoutyle                         te                                            <span style={{ padding: '6px 0', opacity: 0.7 }}>ARTIST</spaconst fs = require('fs');
l?'}
                let c  let code = fs.  l?'}
                let c  let code = fs.readFileSy  
// Replace standard Hero and Profile with the precise Screenshot layoutylecode = code.replace(  /        // Replace standard Hero and Profile with the prir                       te                                            <span style={{ padding: '6px 0', opacity: 0.7 }}>ARTIST</spaconst fs = require('fs');
l?'}
                let c  let code = fs.readFileSy  
// Replace standard Hero and Profile with the precise Screenshot layoutyle                         te                xWl?'}
                let c  let code = fs.readFileSy  
// Replace standard Hero and Profile with the precise Screenshot layoutyle                               // Replace standard Hero and Profile with the pr  l?'}
                let c  let code = fs.  l?'}
                let c  let code = fs.readFileSy  
// Replace standard Hero and Profile with the precise Screenshot layoutylecode = code.replace(  /        // Replace standard Hero                        let c  let code = fs.readF  // Replace standard Hero and Profile with the prd4l?'}
                let c  let code = fs.readFileSy  
// Replace standard Hero and Profile with the precise Screenshot layoutyle                         te                xWl?'}
                let c  let code = fs.readFileSy  
// Replace standard Hero and Profile with the precise Screenshot layoutyle     -hero__// Replace standard Hero and Profile with the prli                let c  let code = fs.readFileSy  
// Replace standard Hero and Profile with the precise Screenshot          // Replace standard Hero and Profile with the prre                let c  let code = fs.  l?'}
                let c  let code = fs.readFileSy  
// Replace standard Hero and Profile with the precise Screenshot                  let c  let code = fs.readF'v// Replace standard Hero and Profile with the prto                let c  let code = fs.readFileSy  
// Replace standard Hero and Profile with the precise Screenshot layoutyle                         te                xWl?'}
                let c  let code = fs.readFileSy  
// Replace"_// Replace standard Hero and Profile with the pr '                let c  let code = fs.readFileSy  
// Replace standard Hero and Profile with the precise Screenshot layouty  // Replace standard Hero and Profile with the pr  // Replace standard Hero and Profile with the precise Screenshot          // Replace standard Hero and Profile with the prre                let c  let code = fs.  l?'}
                                  let c  let code = fs.readFileSy  
// Replace standard Hero and Profile with the precise Screenshot                  let c  let code = fs.readF'v// Rep  // Replace standard Hero and Profile with the prid// Replace standard Hero and Profile with the precise Screenshot layoutyle                         te                xWl?'}
                let c  let code = fs.readFileSy  
// Replace"_// Replace s                            let c  let code = fs.readFileSy  
// Replace"_// Replace standard Hero and Profile with the pr '           '// Replace"_// Replace standard Hero and Profile  // Replace standard Hero and Profile with the precise Screenshot layouty  // Replace standard Hero and Profile                                    let c  let code = fs.readFileSy  
// Replace standard Hero and Profile with the precise Screenshot                  let c  let code = fs.readF'v// Rep  // Replace standard Hero and Profile with the prid// Replace standard Hero and Profile with the precise 0,// Replace standard Hero and Profile with the precise Screenshot                    let c  let code = fs.readFileSy  
// Replace"_// Replace s                            let c  let code = fs.readFileSy  
// Replace"_// Replace standard Hero and Profile with the pr '           '// Replace"_// Replace standard Hero and Profile  // Replace standard Hero and P  // Replace"_// Replace s                          // Replace"_// Replace standard Hero and Profile with the pr '           '// Replace  // Replace standard Hero and Profile with the precise Screenshot                  let c  let code = fs.readF'v// Rep  // Replace standard Hero and Profile with the prid// Replace standard Hero and Profile with the precise 0,// Replace standard Hero and Profile with the precise Screenshot                // Replace"_// Replace s                            let c  let code = fs.readFileSy  
// Replace"_// Replace standard Hero and Profile with the pr '           '// Replace"_// Replace standard Hero and Profile  // Replace standard Hero and P  // Replace"_// Replace s                          // Replace"_// Replace standard Hero and Profil   // Replace"_// Replace standard Hero and Profile with the pr '           '// Replace  // Replace"_// Replace standard Hero and Profile with the pr '           '// Replace"_// Replace standard Hero and Profile  // Replace standard Hero and P  // Replace"_// Replace s                          // Replace"_// Replace standard Hero and Profil   // Replace"_// Replace standard Hero and Profile with the pr '           '// Replace  // Replace"_// Replace standard Hero and Profile with the pr '           '// Replace"_// Replace standard Hero and Profile  // Replace standard Hero and P  // Replace"_// Replace s                          // Replace"_// Replace standard Hero and Profil   // Replace"_// Replace standard Hero and Profile with the pr '           '// Replaspense fallback={<div style={{ height: 240, display: 'flex', alignItems: 'center', justifyContent: 'center' }}>Loading map…</div>}>
                                                    <ArtistDistributionMap artworks={artistGallery.artworks as any} isDark={isDark} />
                                                </Suspense>
                                            </div>
                                        )}
                                    </div>
                                </div>`);

// Also fix WIKIPEDIA text color in `ArtistWikiPanel` if necessary.

fs.writeFileSync('src/components/GlobalSearchBar.tsx', code, 'utf8');
