const fs = require('fs');

async function testParse() {
    const jsdom = require('jsdom');
    const { JSDOM } = jsdom;
    
    // We already identified how to fix checking for images
    console.log("I'm aware the json files had issues. Checking egyptian images...");
}
testParse();
