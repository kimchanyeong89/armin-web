diff --git a/src/components/GlobalNav.tsx b/src/components/GlobalNav.tsx
index 43c05d49..47974e6b 100644
--- a/src/components/GlobalNav.tsx
+++ b/src/components/GlobalNav.tsx
@@ -3,31 +3,60 @@ import { useNavigate, useLocation } from 'react-router-dom';
 import { LoginButton } from './LoginButton';
 import BugReportModal from './BugReportModal';
 import GlobalSearchBar from './GlobalSearchBar';
-import type { SearchableArtwork, Museum } from './GlobalSearchBar';
+import type { GlobalSearchBarProps } from './GlobalSearchBar';
 
 interface GlobalNavProps {
     isAdmin: boolean;
     isModalOpen?: boolean;
-    searchProps?: {
-        onOpenLightbox?: (artwork: SearchableArtwork, openLightbox?: boolean) => void;
-        onNavigateToMuseum?: (museum: { id: string, name: string }, collectionId?: string, artwork?: SearchableArtwork) => void;
-        museums?: Museum[];
-    };
+    skin?: 'default' | 'drawing';
+    onClose?: () => void;
+    searchProps?: Omit<GlobalSearchBarProps, 'isDark' | 'skin' | 'onExpandChange'>;
 }
 
-export const GlobalNav: React.FC<GlobalNavProps> = ({ isAdmin, isModalOpen, searchProps }) => {
+export const GlobalNav: React.FC<GlobalNavProps> = ({ isAdmin, isModalOpen, searchProps, skin = 'default', onClose }) => {
     const navigate = useNavigate();
     const location = useLocation();
     const [isBugReportOpen, setIsBugReportOpen] = useState(false);
     const [isMenuOpen, setIsMenuOpen] = useState(false);
     const [isSearchExpanded, setIsSearchExpanded] = useState(false);
+    const [searchQuery, setSearchQuery] = useState("");
+
+    // Dynamic light/dark mode for the Liquid Glass
+    const [isDark, setIsDark] = useState(() => {
+        try { return localStorage.getItem('homeTheme') !== 'light'; } catch { return true; }
+    });
+
+    useEffect(() => {
+        const updateTheme = () => {
+            try { setIsDark(localStorage.getItem('homeTheme') !== 'light'); } catch { setIsDark(true); }
+        };
+        // Listen to native storage events globally
+        window.addEventListener('storage', updateTheme);
+        window.addEventListener('theme-changed', updateTheme);
+        return () => {
+            window.removeEventListener('storage', updateTheme);
+            window.removeEventListener('theme-changed', updateTheme);
+        };
+    }, []);
 
     const currentPath = location.pathname;
+    const isDrawingSkin = skin === 'drawing';
 
     const [position, setPosition] = useState(() => {
         try {
             const saved = localStorage.getItem('globalNavPos');
-            if (saved) return JSON.parse(saved);
+            if (saved) {
+                const pos = JSON.parse(saved);
+                // Clamp saved position to reasonable viewport bounds to prevent off-screen drift
+                const maxAbsX = typeof window !== 'undefined' ? window.innerWidth * 0.4 : 400;
+                const maxAbsY = typeof window !== 'undefined' ? window.innerHeight * 0.4 : 300;
+                // Allow positive Y only up to a very small amount (since it goes off the bottom edge)
+                if (Math.abs(pos.x) < maxAbsX && pos.y >= -maxAbsY && pos.y <= 24) {
+                    return pos;
+                }
+                // Saved position is out of reasonable range — reset to center
+                localStorage.removeItem('globalNavPos');
+            }
         } catch (e) {
             console.error('Failed to load nav pos', e);
         }
@@ -78,13 +107,28 @@ export const GlobalNav: React.FC<GlobalNavProps> = ({ isAdmin, isModalOpen, sear
     }, [position]);
 
     // Track when inline search expands so we can close the menu
+    const preSearchPos = useRef<{ x: number, y: number } | null>(null);
     useEffect(() => {
         const onSearchOpen = () => {
             setIsMenuOpen(false);
             setIsBugReportOpen(false);
             setIsSearchExpanded(true);
+            setPosition((prev: { x: number, y: number }) => {
+                preSearchPos.current = prev;
+                // Move to near the top of the scren
+                const targetY = isDrawingSkin ? 0 : -(window.innerHeight - 150);
+                const currentNavHeight = wrapperRef.current?.offsetHeight || 60;
+                const minAllowedY = -(window.innerHeight - currentNavHeight - 52);
+                return isDrawingSkin ? prev : { x: 0, y: Math.max(targetY, minAllowedY) };
+            });
+        };
+        const onSearchClose = () => {
+            setIsSearchExpanded(false);
+            if (preSearchPos.current) {
+                setPosition(preSearchPos.current);
+                preSearchPos.current = null;
+            }
         };
-        const onSearchClose = () => setIsSearchExpanded(false);
         window.addEventListener('global-search-expanded', onSearchOpen);
         window.addEventListener('global-search-collapsed', onSearchClose);
         return () => {
@@ -102,19 +146,27 @@ export const GlobalNav: React.FC<GlobalNavProps> = ({ isAdmin, isModalOpen, sear
         const start = Date.now();
 
         const checkBounds = () => {
+            if (isDrawingSkin) return;
             if (!wrapperRef.current) return;
             const width = wrapperRef.current.offsetWidth;
             const height = wrapperRef.current.offsetHeight;
-            const maxX = window.innerWidth - width - 20;
-            const maxY = window.innerHeight - height - 20;
+
+            // Parent is at left: 50%, bottom: 32px
+            const maxDragX = window.innerWidth / 2 - width / 2 - 20;
+            const minDragX = -(window.innerWidth / 2 - width / 2 - 20);
+
+            const maxDragY = 12; // can only go down by 12px before hitting bottom edge (32 - 20)
+            const minDragY = -(window.innerHeight - height - 52); // can go up to top edge
 
             setPosition((prev: { x: number; y: number }) => {
                 let newX = prev.x;
                 let newY = prev.y;
                 let changed = false;
 
-                if (newX > maxX) { newX = Math.max(20, maxX); changed = true; }
-                if (newY > maxY) { newY = Math.max(20, maxY); changed = true; }
+                if (newX > maxDragX) { newX = maxDragX; changed = true; }
+                if (newX < minDragX) { newX = minDragX; changed = true; }
+                if (newY > maxDragY) { newY = maxDragY; changed = true; }
+                if (newY < minDragY) { newY = minDragY; changed = true; }
 
                 return changed ? { x: newX, y: newY } : prev;
             });
@@ -130,6 +182,7 @@ export const GlobalNav: React.FC<GlobalNavProps> = ({ isAdmin, isModalOpen, sear
     }, [isMenuOpen, isSearchExpanded]);
 
     const handleMouseDown = (e: React.MouseEvent) => {
+        if (isDrawingSkin) return;
         const target = e.target as HTMLElement;
         // Do not trigger drag on interactive elements
         if (target.tagName.toLowerCase() === 'button' || target.tagName.toLowerCase() === 'input' || target.closest('button')) {
@@ -139,22 +192,25 @@ export const GlobalNav: React.FC<GlobalNavProps> = ({ isAdmin, isModalOpen, sear
         if (!wrapperRef.current) return;
         const rect = wrapperRef.current.getBoundingClientRect();
 
-        // Ensure nav isn't dragged entirely out of view.
-        const marginX = rect.width * 0.5;
-        const marginY = 40;
+        // Parent is at left: 50%, bottom: 32px
+        const width = rect.width;
+        const height = rect.height;
 
-        const minX = position.x - rect.left - marginX;
-        const maxX = position.x + (window.innerWidth - rect.right) + marginX;
+        const maxDragX = window.innerWidth / 2 - width / 2 - 20;
+        const minDragX = -(window.innerWidth / 2 - width / 2 - 20);
 
-        const minY = position.y - rect.top; // Don't drag up beyond top zero
-        const maxY = position.y + (window.innerHeight - rect.bottom) + marginY;
+        const maxDragY = 12;
+        const minDragY = -(window.innerHeight - height - 52);
 
         dragRef.current = {
             startX: e.clientX,
             startY: e.clientY,
             initialX: position.x,
             initialY: position.y,
-            minX, maxX, minY, maxY
+            minX: minDragX,
+            maxX: maxDragX,
+            minY: minDragY,
+            maxY: maxDragY
         };
         isDragging.current = true;
         wrapperRef.current.style.cursor = 'grabbing';
@@ -163,9 +219,10 @@ export const GlobalNav: React.FC<GlobalNavProps> = ({ isAdmin, isModalOpen, sear
     const wrapperStyle: React.CSSProperties = {
         position: 'relative',
         display: 'inline-flex',
-        flexDirection: 'row', // align items left to right
-        transform: `translate(${position.x}px, ${position.y}px)`,
-        cursor: 'grab',
+        flexDirection: 'row',
+        transform: isDrawingSkin ? 'none' : `translate(${position.x}px, ${position.y}px)`,
+        transition: isDragging.current ? 'none' : (isDrawingSkin ? 'transform 0.4s ease-in-out' : 'transform 1.1s cubic-bezier(0.22, 1, 0.36, 1)'),
+        cursor: isDrawingSkin ? 'default' : 'grab',
         zIndex: 100000,
         WebkitUserSelect: 'none',
         userSelect: 'none'
@@ -178,33 +235,44 @@ export const GlobalNav: React.FC<GlobalNavProps> = ({ isAdmin, isModalOpen, sear
         gap: "4px",
         padding: '6px',
         zIndex: 100,
-        transition: 'all 0.6s cubic-bezier(0.2, 0.8, 0.2, 1)',
+        transition: isDrawingSkin ? 'all 0.4s ease-in-out' : 'all 1.2s cubic-bezier(0.22, 1, 0.36, 1)',
         minHeight: '60px',
+        ...(isDrawingSkin && {
+            background: '#FFFFFF',
+            border: '2.5px solid #111111',
+            borderRadius: 9999,
+            boxShadow: '4px 4px 0px 0px rgba(17,17,17,1)',
+            filter: 'url(#dg-sketch-ui)',
+        })
     };
 
     const backgroundStyle: React.CSSProperties = {
         position: 'absolute',
         top: 0, left: 0, right: 0, bottom: 0,
-        background: isModalOpen ? 'rgba(255, 255, 255, 0.4)' : 'rgba(255, 255, 255, 0.75)',
-        backdropFilter: 'blur(30px) saturate(200%)',
-        WebkitBackdropFilter: 'blur(30px) saturate(200%)',
+        background: isDark
+            ? (isModalOpen ? 'rgba(6, 6, 5, 0.88)' : 'rgba(6, 6, 5, 0.95)')
+            : (isModalOpen ? 'rgba(255, 255, 255, 0.75)' : 'rgba(255, 255, 255, 0.88)'),
+        backdropFilter: 'blur(40px) saturate(200%)',
+        WebkitBackdropFilter: 'blur(40px) saturate(200%)',
         borderRadius: '100px',
-        border: '1px solid rgba(255, 255, 255, 0.3)',
-        boxShadow: '0 4px 15px rgba(0, 0, 0, 0.05), inset 0 1px 1px rgba(255, 255, 255, 0.6)',
+        border: isDark ? '1px solid rgba(201, 165, 90, 0.22)' : '1px solid rgba(0, 0, 0, 0.10)',
+        boxShadow: isDark
+            ? '0 8px 40px rgba(0, 0, 0, 0.7), 0 1px 0 rgba(255,255,255,0.04) inset'
+            : '0 8px 32px rgba(0, 0, 0, 0.10), 0 1px 0 rgba(255,255,255,0.6) inset',
         zIndex: -1,
         pointerEvents: 'none',
-        transition: 'all 0.6s cubic-bezier(0.2, 0.8, 0.2, 1)',
+        transition: 'background 1.1s cubic-bezier(0.22, 1, 0.36, 1), box-shadow 1.1s cubic-bezier(0.22, 1, 0.36, 1), border-color 1.1s cubic-bezier(0.22, 1, 0.36, 1)',
     };
 
     const getNavItemStyle = (isActive: boolean, isAccent: boolean = false): React.CSSProperties => {
         let bg = 'transparent';
-        let color = '#555';
+        let color = isDark ? '#d4cfc7' : '#2a2520';
         if (isAccent) {
-            bg = '#e8fb36';
-            color = '#000';
+            bg = isDark ? 'rgba(201,165,90,0.18)' : 'rgba(140,110,40,0.12)';
+            color = isDark ? '#c9a55a' : '#a27e36';
         } else if (isActive) {
-            bg = '#e5e7eb';
-            color = '#000';
+            bg = isDark ? 'rgba(201,165,90,0.15)' : 'rgba(140,110,40,0.10)';
+            color = '#c9a55a';
         }
 
         return {
@@ -212,7 +280,7 @@ export const GlobalNav: React.FC<GlobalNavProps> = ({ isAdmin, isModalOpen, sear
             background: bg,
             color: color,
             fontSize: '13px',
-            fontWeight: 800,
+            fontWeight: 700,
             letterSpacing: '-0.2px',
             border: 'none',
             borderRadius: '100px',
@@ -220,16 +288,17 @@ export const GlobalNav: React.FC<GlobalNavProps> = ({ isAdmin, isModalOpen, sear
             display: 'inline-flex',
             alignItems: 'center',
             justifyContent: 'center',
-            transition: 'background 0.2s ease, color 0.2s, transform 0.1s',
+            transition: 'background 0.35s ease, color 0.35s ease, transform 0.1s',
             fontFamily: "'Inter', Arial, Helvetica, sans-serif",
             whiteSpace: 'nowrap',
         };
     };
 
+
     return (
-        <div ref={wrapperRef} style={wrapperStyle} onMouseDown={handleMouseDown}>
+        <div id="global-nav-wrapper" ref={wrapperRef} style={wrapperStyle} onMouseDown={handleMouseDown}>
             <div style={containerStyle}>
-                <div style={backgroundStyle} />
+                {!isDrawingSkin && <div style={backgroundStyle} />}
                 {/* Profile Avatar always on Left */}
                 <div
                     style={{ display: 'flex', alignItems: 'center', margin: '0 4px', zIndex: 10 }}
@@ -238,15 +307,14 @@ export const GlobalNav: React.FC<GlobalNavProps> = ({ isAdmin, isModalOpen, sear
                     <LoginButton />
                 </div>
 
-                {/* Inline Search Bar (Transitions to/from Yellow Circle) */}
-                <div style={{ marginLeft: '4px', marginRight: '4px' }}>
-                    <GlobalSearchBar
-                        inlineMode={true}
-                        onOpenLightbox={searchProps?.onOpenLightbox || (() => { })}
-                        onNavigateToMuseum={searchProps?.onNavigateToMuseum || (() => { })}
-                        museums={searchProps?.museums}
-                        isModalOpen={isModalOpen}
-                    />
+                <div style={{
+                    width: isSearchExpanded ? 'min(420px, calc(100vw - 40px))' : '50px',
+                    opacity: 1,
+                    overflow: 'visible',
+                    transition: isDrawingSkin ? 'width 0.4s ease-in-out' : 'width 1.3s cubic-bezier(0.22, 1, 0.36, 1)',
+                    pointerEvents: 'auto'
+                }}>
+                    <GlobalSearchBar inlineMode {...searchProps} isDark={isDark} skin={isDrawingSkin ? 'drawing' : 'default'} />
                 </div>
 
                 {/* Horizontal Sliding Menu Area */}
@@ -257,12 +325,12 @@ export const GlobalNav: React.FC<GlobalNavProps> = ({ isAdmin, isModalOpen, sear
                     overflow: 'hidden',
                     maxWidth: isMenuOpen ? '600px' : '0px',
                     opacity: isMenuOpen ? 1 : 0,
-                    transition: 'max-width 0.55s cubic-bezier(0.34, 1.56, 0.64, 1), opacity 0.4s cubic-bezier(0.2, 0.8, 0.2, 1)',
+                    transition: isDrawingSkin ? 'max-width 0.4s ease-in-out, opacity 0.3s' : 'max-width 1.3s cubic-bezier(0.22, 1, 0.36, 1), opacity 0.9s cubic-bezier(0.22, 1, 0.36, 1)',
                 }}>
                     {isAdmin && (
                         <button
                             onClick={() => navigate('/admin')}
-                            style={{ ...getNavItemStyle(currentPath === '/admin'), transition: 'background 0.2s ease, color 0.2s, transform 0.3s, opacity 0.4s', transform: isMenuOpen ? 'translateX(0)' : 'translateX(-12px)', opacity: isMenuOpen ? 1 : 0 }}
+                            style={{ ...getNavItemStyle(currentPath === '/admin'), transition: 'background 0.35s ease, color 0.35s, transform 1.3s cubic-bezier(0.22, 1, 0.36, 1), opacity 1.0s', transform: isMenuOpen ? 'translateX(0)' : 'translateX(-20px)', opacity: isMenuOpen ? 1 : 0 }}
                             onMouseDown={(e) => { e.stopPropagation(); e.nativeEvent.stopImmediatePropagation(); }}
                         >
                             ADMIN PROFILE
@@ -270,21 +338,21 @@ export const GlobalNav: React.FC<GlobalNavProps> = ({ isAdmin, isModalOpen, sear
                     )}
                     <button
                         onClick={() => navigate('/mypage')}
-                        style={{ ...getNavItemStyle(currentPath === '/mypage'), transition: 'background 0.2s ease, color 0.2s, transform 0.35s 0.03s, opacity 0.4s 0.03s', transform: isMenuOpen ? 'translateX(0)' : 'translateX(-12px)', opacity: isMenuOpen ? 1 : 0 }}
+                        style={{ ...getNavItemStyle(currentPath === '/mypage'), transition: 'background 0.35s ease, color 0.35s, transform 1.3s cubic-bezier(0.22, 1, 0.36, 1) 0.06s, opacity 1.0s 0.06s', transform: isMenuOpen ? 'translateX(0)' : 'translateX(-20px)', opacity: isMenuOpen ? 1 : 0 }}
                         onMouseDown={(e) => { e.stopPropagation(); e.nativeEvent.stopImmediatePropagation(); }}
                     >
                         MY PAGE
                     </button>
                     <button
                         onClick={() => window.dispatchEvent(new CustomEvent('toggle-community-panel'))}
-                        style={{ ...getNavItemStyle(false), transition: 'background 0.2s ease, color 0.2s, transform 0.35s 0.06s, opacity 0.4s 0.06s', transform: isMenuOpen ? 'translateX(0)' : 'translateX(-12px)', opacity: isMenuOpen ? 1 : 0 }}
+                        style={{ ...getNavItemStyle(false), transition: 'background 0.35s ease, color 0.35s, transform 1.3s cubic-bezier(0.22, 1, 0.36, 1) 0.12s, opacity 1.0s 0.12s', transform: isMenuOpen ? 'translateX(0)' : 'translateX(-20px)', opacity: isMenuOpen ? 1 : 0 }}
                         onMouseDown={(e) => { e.stopPropagation(); e.nativeEvent.stopImmediatePropagation(); }}
                     >
                         COMMUNITY
                     </button>
                     <button
                         onClick={() => setIsBugReportOpen(!isBugReportOpen)}
-                        style={{ ...getNavItemStyle(isBugReportOpen, true), transition: 'background 0.2s ease, color 0.2s, transform 0.35s 0.09s, opacity 0.4s 0.09s', transform: isMenuOpen ? 'translateX(0)' : 'translateX(-12px)', opacity: isMenuOpen ? 1 : 0 }}
+                        style={{ ...getNavItemStyle(isBugReportOpen, true), transition: 'background 0.35s ease, color 0.35s, transform 1.3s cubic-bezier(0.22, 1, 0.36, 1) 0.18s, opacity 1.0s 0.18s', transform: isMenuOpen ? 'translateX(0)' : 'translateX(-20px)', opacity: isMenuOpen ? 1 : 0 }}
                         onMouseDown={(e) => { e.stopPropagation(); e.nativeEvent.stopImmediatePropagation(); }}
                     >
                         BUG REPORT
@@ -298,7 +366,6 @@ export const GlobalNav: React.FC<GlobalNavProps> = ({ isAdmin, isModalOpen, sear
                         const opening = !isMenuOpen;
                         setIsMenuOpen(opening);
                         setIsBugReportOpen(false);
-                        // When menu opens, collapse the search bar
                         if (opening && isSearchExpanded) {
                             window.dispatchEvent(new CustomEvent('global-nav-close-search'));
                         }
@@ -308,19 +375,28 @@ export const GlobalNav: React.FC<GlobalNavProps> = ({ isAdmin, isModalOpen, sear
                         width: '48px',
                         height: '48px',
                         borderRadius: '48px',
-                        background: isMenuOpen ? 'rgba(0,0,0,0.1)' : 'transparent',
+                        background: isMenuOpen
+                            ? (isDark ? 'rgba(201,165,90,0.15)' : 'rgba(140,110,40,0.10)')
+                            : 'transparent',
                         border: 'none',
                         cursor: 'pointer',
                         display: 'flex',
                         alignItems: 'center',
                         justifyContent: 'center',
-                        transition: 'background 0.3s ease',
+                        transition: 'background 0.4s cubic-bezier(0.16, 1, 0.3, 1)',
                         marginLeft: '2px',
+                        flexShrink: 0,
+                    }}
+                    onMouseEnter={(e) => {
+                        if (!isMenuOpen) e.currentTarget.style.background = isDark ? 'rgba(201,165,90,0.08)' : 'rgba(0,0,0,0.05)';
+                    }}
+                    onMouseLeave={(e) => {
+                        if (!isMenuOpen) e.currentTarget.style.background = 'transparent';
                     }}
-                    onMouseEnter={(e) => !isMenuOpen && (e.currentTarget.style.background = 'rgba(0,0,0,0.05)')}
-                    onMouseLeave={(e) => !isMenuOpen && (e.currentTarget.style.background = 'transparent')}
                 >
-                    <svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="#333" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round">
+                    <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="#c9a55a" strokeWidth="2.2" strokeLinecap="round" strokeLinejoin="round"
+                        style={{ transition: 'transform 0.5s cubic-bezier(0.16, 1, 0.3, 1), opacity 0.4s' }}
+                    >
                         {isMenuOpen ? (
                             <>
                                 <line x1="18" y1="6" x2="6" y2="18"></line>
